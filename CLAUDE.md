# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Status

This repository is currently empty (no commits, no source files). This file is a placeholder.

Once the project has real code, structure, and tooling in place, regenerate this file (`/init`) so it can document:

- Build, lint, and test commands (including how to run a single test)
- High-level architecture and how major components fit together
